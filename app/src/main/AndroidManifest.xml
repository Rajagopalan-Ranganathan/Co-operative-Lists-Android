<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fi.aalto.msp2017.shoppinglist">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FireActivity" />
        <activity android:name=".AccountActivity" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name=".ShoppingListActivity"
            android:label="@string/title_activity_shopping_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ShoppingListActivityHG" />
        <activity android:name=".ShoppingListActivityTab" />
        <activity android:name=".ListItemDetailsActivity" />
        <activity android:name=".ShoppingListMemberActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".CircleTransform" />
    </application>

</manifest>